#MAILTO=""
30 * * * *    /usr/bin/ntpdate -b -s -u pool.ntp.org
*  * * * *    /etc/init.d/save-rtc.sh
*  * * * *    /opt/bin/recorder.sh --mngaddr --intf eth0
*/5 * * * *   /opt/bin/recorder.sh --clean --brate 48
